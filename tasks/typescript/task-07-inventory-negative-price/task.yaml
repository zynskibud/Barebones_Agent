name: inventory-negative-price
type: feature
prompt: "In inventory.ts, make `addItem` throw a `RangeError` when the price is negative. A price of zero is still allowed."
test_command: "node --test hidden_tests/*.test.ts"
limits:
  max_turns: 40
  max_seconds: 600
