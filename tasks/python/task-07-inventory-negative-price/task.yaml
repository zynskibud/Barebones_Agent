name: inventory-negative-price
type: feature
prompt: "In inventory.py, make `add_item` raise `ValueError` when the price is negative. A price of zero is still allowed."
test_command: "python -m pytest hidden_tests -q"
limits:
  max_turns: 20
  max_seconds: 300
