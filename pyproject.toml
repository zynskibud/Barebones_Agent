[project]
name = "barebones-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "e2b>=2.51.0",
    "pytest>=9.1.1",
    "pyyaml>=6.0.3",
]

[tool.pytest.ini_options]
norecursedirs = ["tasks", "runs", ".venv"]
